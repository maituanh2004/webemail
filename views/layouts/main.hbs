<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        /* ... existing styles ... */
        
        .layout-container {
            display: flex;
            min-height: calc(100vh - 70px);
            margin-top: 70px;
        }

        .sidebar {
            width: 250px;
            background-color: #1A1A2E;
            padding: 20px;
            position: fixed;
            height: calc(100vh - 70px);
        }

        .sidebar-menu {
            list-style: none;
            padding: 0;
        }

        .sidebar-menu a {
            display: block;
            padding: 15px;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            margin-bottom: 10px;
            transition: background-color 0.3s;
        }

        .sidebar-menu a:hover {
            background-color: #25253C;
        }

        .main-content {
            flex: 1;
            margin-left: 250px;
            padding: 20px;
        }
    </style>
</head>
<body>
    {{> header loggedIn=loggedIn userFullname=userFullname}}
    
    {{#if loggedIn}}
    <div class="layout-container">
        <nav class="sidebar">
            <ul class="sidebar-menu">
                <li><a href="/compose">Compose</a></li>
                <li><a href="/inbox">Inbox</a></li>
                <li><a href="/outbox">Outbox</a></li>
            </ul>
        </nav>
        <main class="main-content">
            {{{body}}}
        </main>
    </div>
    {{else}}
        <div class="container">
            {{{body}}}
        </div>
    {{/if}}
    
</body>
</html>
